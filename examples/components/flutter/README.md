# LUI component gallery Flutter host

This directory is only the Flutter host shell. The shared application model,
reducer, and UI source live in `../lg/components`.

See [`../README.md`](../README.md) for build, test, and run commands.
