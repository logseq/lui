name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ocaml-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: "5.5"
      - name: Cache opam switch
        uses: actions/cache@v4
        with:
          path: ~/.opam
          key: opam-${{ runner.os }}-5.5-${{ hashFiles('*.opam*', 'dune-project') }}
      - name: Cache dune
        uses: actions/cache@v4
        with:
          path: ~/.cache/dune
          key: dune-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            dune-${{ runner.os }}-
      - name: Enable dune cache
        run: |
          echo "DUNE_CACHE=enabled" >> "$GITHUB_ENV"
          echo "DUNE_CACHE_TRANSPORT=direct" >> "$GITHUB_ENV"
      # The ocaml-signal pin lives in lui.opam.locked (pin-depends);
      # --locked makes opam read it instead of lui.opam.
      - name: Install lui dependencies
        run: opam install . --deps-only --with-test -y --locked
      - name: Schema tests
        run: node --test tooling/test/*.mjs
      - name: OCaml tests
        run: opam exec -- dune build --action-stderr-on-success=must-be-empty @runtest

  apple-tests:
    # Package.swift requires swift-tools-version 6.2 (Xcode 26).
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Cache SwiftPM build
        uses: actions/cache@v4
        with:
          path: .build
          key: swiftpm-${{ runner.os }}-${{ hashFiles('Package.swift') }}-${{ github.sha }}
          restore-keys: |
            swiftpm-${{ runner.os }}-${{ hashFiles('Package.swift') }}-
      - run: swift test
