name: lui_flutter_backend
description: Flutter host backend for the LG LUI runtime.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  ffi: ^2.1.4
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
